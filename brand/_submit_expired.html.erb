<% heading = t('hamilton.signing_link_expired', default: I18n.locale.to_s.start_with?('es') ? 'El enlace para firmar venció' : 'Signing link expired') %>
<% content_for(:html_title, "#{heading} | Hamilton Signing") %>
<%= render 'shared/terminal_state', heading:, message: t('form_expired_at_html', time: l(@submitter.submission.expire_at, format: :long)), account: @submitter.account, link_path: '/start' %>
