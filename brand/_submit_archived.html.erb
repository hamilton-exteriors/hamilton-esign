<% heading = t('hamilton.form_unavailable', default: I18n.locale.to_s.start_with?('es') ? 'Formulario no disponible' : 'Form unavailable') %>
<% content_for(:html_title, "#{heading} | Hamilton Signing") %>
<%= render 'shared/terminal_state', heading:, message: t('form_has_been_deleted_by_html', name: @submitter.account.name), account: @submitter.account, link_path: '/start' %>
