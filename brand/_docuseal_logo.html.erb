<%# Logo only. The wordmark already reads "Hamilton Exteriors"; setting the
    product name beside it repeats the same words twice at the top of the page
    and reads as an unfinished template. %>
<a href="<%= root_path %>" class="hx-brand" aria-label="Hamilton Exteriors">
  <%= render 'shared/logo', class: 'hx-brand-mark' %>
</a>
