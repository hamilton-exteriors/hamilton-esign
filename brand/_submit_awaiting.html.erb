<% heading = t('hamilton.waiting_for_signers', default: I18n.locale.to_s.start_with?('es') ? 'Esperando a los demás firmantes' : 'Waiting for other signers') %>
<% content_for(:html_title, "#{heading} | Hamilton Signing") %>
<%= render 'shared/terminal_state', heading:, message: t('awaiting_completion_by_the_other_party'), account: @submitter.account, link_path: '/start' %>
